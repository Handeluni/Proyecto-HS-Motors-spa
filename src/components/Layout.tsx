import { Menu, X } from 'lucide-react'
import { useState } from 'react'
import { Link, NavLink, Outlet } from 'react-router-dom'
const navigation = [{to:'/',label:'Inicio'},{to:'/vehicles',label:'Vehículos'},{to:'/services',label:'Servicios'},{to:'/about',label:'Nosotros'}]
export function Layout() { const [open,setOpen]=useState(false); const close=()=>setOpen(false); return <><header className="header"><div className="container nav"><Link className="brand" to="/" onClick={close}><span>HS</span> MOTORS</Link><button className="menu" aria-label="Abrir navegación" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button><nav className={open?'navlinks open':'navlinks'}>{navigation.map(item=><NavLink end={item.to==='/' } key={item.to} to={item.to} onClick={close}>{item.label}</NavLink>)}<Link className="button button-small" to="/quote" onClick={close}>Cotizar</Link></nav></div></header><main><Outlet/></main><footer><div className="container footer-grid"><div><Link className="brand light" to="/"><span>HS</span> MOTORS</Link><p>Movilidad que inspira. Vehículos y servicios diseñados alrededor de ti.</p></div><div><strong>Explora</strong><Link to="/vehicles">Catálogo</Link><Link to="/services">Servicios</Link></div><div><strong>Atención</strong><Link to="/quote">Solicitar cotización</Link><Link to="/test-drive">Prueba de manejo</Link></div></div><div className="container copyright">© {new Date().getFullYear()} HS Motors. Vehículos y servicios ficticios para demostración.</div></footer></> }
